#pragma once
#include <time.h>
#include "validation.h"

//This function generates a random number between 1 and 15 and returns it
int generateRandomNumber();
//This function generates a random column and returns it
int* generateRandomNumbers();